import RPi.GPIO as GPIO
import time

GPIO.setmode(GPIO.BCM)
GPIO.setup(17, GPIO.OUT)

while True:
    GPIO.output(17, GPIO.HIGH)  # fans ON
    time.sleep(5)

    GPIO.output(17, GPIO.LOW)   # fans OFF
    time.sleep(5)
